# Fido Scanner V2 — Docs site

Static landing page for the [Fido Scanner V2](https://github.com/The-LukeZ/fido-scanner-v2) Android app. Built with [Astro](https://astro.build) and [Paraglide JS](https://inlang.com/m/gerre34r/library-inlang-paraglideJs) for i18n.

Deployed to GitHub Pages at https://fidoscan.thelukez.com/.

## Commands

Run from this directory (`docs/`):

| Command        | Action                                              |
| :------------- | :-------------------------------------------------- |
| `pnpm install` | Install dependencies                                |
| `pnpm dev`     | Start local dev server at `localhost:4321`          |
| `pnpm build`   | Compile messages + build static output to `./dist/` |
| `pnpm preview` | Preview the production build locally                |

## Project structure

```
docs/
├── messages/
│   ├── en.json          # English translations (source of truth)
│   └── de.json          # German translations
├── project.inlang/
│   └── settings.json    # inlang project config (locales, plugins)
├── src/
│   ├── assets/
│   │   └── icon-512.png         # Product icon (processed by astro:assets)
│   ├── pages/
│   │   ├── index.astro          # Root redirect — detects browser language
│   │   └── [locale]/
│   │       └── index.astro      # Main page, one static file per locale
│   └── paraglide/               # Generated by paraglide-js — do not edit
├── public/                      # Static assets served as-is
│   ├── favicon.ico
│   ├── apple-touch-icon.png
│   ├── icon-192.png
│   ├── icon-192-maskable.png
│   ├── icon-512-maskable.png
│   └── CNAME
└── astro.config.mjs
```

## i18n

Locales are defined in `project.inlang/settings.json`. The build produces one static HTML file per locale (e.g. `/en/index.html`, `/de/index.html`). The root `/index.html` redirects to the appropriate locale based on `navigator.language`. A language switcher `<select>` in the top-right corner lets users switch locale at runtime.

**To add a new locale:**

1. Add it to `locales` in `project.inlang/settings.json` and `astro.config.mjs` (`i18n.locales` and `sitemap.i18n.locales`)
2. Create `messages/{locale}.json` with all keys from `messages/en.json` translated
3. Run `pnpm build` — the new locale is picked up automatically everywhere (routes, language switcher, hreflang tags, root redirect)

**To add a new translatable string:**

1. Add the key to `messages/en.json` and all other locale files
2. Use `m.your_key()` in `src/pages/[locale]/index.astro`

## Architecture notes

- `output: "static"` — fully pre-rendered, no server runtime
- Paraglide's URL strategy is skipped during SSG builds (`isServer = true`), so locale is set explicitly via `overwriteGetLocale(() => assertIsLocale(locale))` in each page's frontmatter using the `[locale]` route param
- Dynamic JS strings (error messages, etc.) are embedded into a `data-strings` attribute at build time and read by the client script at runtime
- Release info (version, download URL, release notes) is fetched from the GitHub API (`/repos/The-LukeZ/fido-scanner-v2/releases/latest`) at runtime; release notes markdown is rendered client-side with [`marked`](https://marked.js.org/)
